
          
name: test-ci

on:
  push:
    branches: [ $default-branch ]
  pull_request:
    branches: [ $default-branch ]
    
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
        
      - name: one-line script
        run: ehco Hey! How have you been?
        
      - name: $default-branch
        run: echo Sometimes I miss when we are studying together. $default-branch
        
